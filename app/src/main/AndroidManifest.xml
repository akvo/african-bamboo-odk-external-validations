<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Required for Android 11+ to query apps that can handle map URLs -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".AfriBamODKValidatorApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AfriBamODKValidator">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AfriBamODKValidator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".validation.PolygonValidationActivity"
            android:exported="true"
            android:theme="@style/Theme.AfriBamODKValidator.AppCompat">
            <intent-filter>
                <action android:name="org.akvo.afribamodkvalidator.VALIDATE_POLYGON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Blur Validation: custom camera app for ODK Collect -->
        <activity
            android:name=".validation.BlurValidationActivity"
            android:exported="true"
            android:theme="@style/Theme.AfriBamODKValidator.AppCompat">
            <intent-filter>
                <action android:name="android.media.action.IMAGE_CAPTURE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".validation.MapPreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.AfriBamODKValidator.AppCompat" />

        <activity
            android:name=".validation.TilePreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.AfriBamODKValidator.AppCompat" />
        <!-- FileProvider for camera image capture -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>